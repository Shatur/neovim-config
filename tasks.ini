[project-clean-build]
command=rm -r $(VIM_ROOT)/../build-$(VIM_PRONAME)-$(VIM_PROFILE)
silent=1

[project-open-build]
command=xdg-open $(VIM_ROOT)/../build-$(VIM_PRONAME)-$(VIM_PROFILE)
silent=1

[project-open-source]
command=xdg-open $(VIM_ROOT)
silent=1
